# alias v='vim'
alias ev='vim ~/.vimrc'
alias ez='vim ~/.zshrc'

# disabling in favor of neovim
# function v() {
#     if [ -d .venv ]; then
#         pipenv run vim $@
#     else
#         vim $@
#     fi
# }
